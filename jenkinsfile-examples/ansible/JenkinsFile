pipeline {
    agent none
    stages {
        stage('Staging'){
            agent { label 'docker' }
            steps {
                checkout scm
                sh 'find ./ -type f'
                sh 'pip install -r ansible25.requirements'
            }
        }
    }
}
