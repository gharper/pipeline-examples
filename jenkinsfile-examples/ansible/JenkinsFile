pipeline {
    agent none
    stages {
        stage('Staging'){
            agent { label 'docker' }
            steps {
                checkout scm
                script {
                    sh 'sudo apt-get update'
                    sh 'sudo apt-get -y install libssl-dev'
                    sh 'sudo /usr/local/bin/pip install -U pip setuptools wheel'
                    sh 'sudo /usr/local/bin/pip install -Uv m2crypto'
                    sh 'sudo /usr/local/bin/pip install -Uv m2crypto'
                    sh 'sudo /usr/local/bin/pip install -r ./jenkinsfile-examples/ansible/ansible25.requirements'
                    // sh 'pip install ansible==2.5.2'
                    sh 'ansible --version'
                }
            }
        }
    }
}
